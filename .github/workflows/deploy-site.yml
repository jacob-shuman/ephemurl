name: Deploy Ephemurl Site

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "apps/site/**"

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      NODE_OPTIONS: --max_old_space_size=4096
      ADAPTER: cloudflare

    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm 🏗
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --filter ./apps/site --filter ./shared/*

      - name: Build utils
        run: pnpm utils build

      - name: Build site
        run: pnpm site build

      # TODO: disable this once docker deployment is fixed
      # - name: Deploy to cloudflare
      #   uses: cloudflare/pages-action@1
      #   with:
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     projectName: ephemurl-site
      #     directory: ./apps/site/dist
      #     gitHubToken: ${{ secrets.GITHUB_TOKEN }}
      #     wranglerVersion: "3"

      # TODO: enable this once docker deployment is fixed
      # - name: Deploy to cloudflare
      #   uses: cloudflare/wrangler-action@v3
      #   with:
      #     workingDirectory: ./apps/site
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     command: pnpx wrangler pages deploy ./dist --project-name=ephemurl-site --commit-dirty=true
      # TODO: switch to this later
      # command: pages deploy dist --project-name=${{ vars.CLOUDFLARE_PROJECT_NAME }} --commit-dirty=true

      # - name: Login to cloudflare
      #   run: echo ${{ secrets.CLOUDFLARE_API_TOKEN }} | wrangler login --api-key

      - name: Deploy to cloudflare
        run: pnpx wrangler pages deploy ./apps/site/dist --project-name=ephemurl-site --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{secrets.CLOUDFLARE_API_TOKEN}}
          CLOUDFLARE_ACCOUNT_ID: ${{secrets.CLOUDFLARE_ACCOUNT_ID}}

      - name: Log in to docker hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Build docker image
        run: docker build . -f ./Dockerfile.ci --build-arg APP=site -t jacobshuman/ephemurl-site:latest

      - name: Push docker image
        run: docker push jacobshuman/ephemurl-site:latest

      # - name: Build and push docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #     no-cache: true
      #     build-args: |
      #       APP=site
      #     push: true
      #     file: Dockerfile.ci
      #     tags: jacobshuman/ephemurl-site
